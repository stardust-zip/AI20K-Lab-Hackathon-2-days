# ============================================================
# Vinmec AI Triage – Python dependencies
# ============================================================

# --- Web framework & server ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # required for FastAPI form/file uploads

# --- Data validation ---
pydantic>=2.7.0
pydantic-settings>=2.2.0

# --- AI / LLM ---
openai>=1.30.0
anthropic>=0.28.0                # kept for potential fallback / multi-provider

# --- Observability ---
langfuse>=2.36.0

# --- De-identification (PII/PHI) ---
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354
spacy>=3.7.4

# --- Database ---
sqlalchemy>=2.0.30
psycopg2-binary>=2.9.9           # sync psycopg2 used directly for pgvector raw SQL
asyncpg>=0.29.0                  # async PostgreSQL driver (optional, for future async queries)
pgvector>=0.3.2                  # pgvector Python helpers

# --- Supabase client (REST / Realtime) ---
supabase>=2.4.0

# --- HTTP client ---
httpx>=0.27.0

# --- Rate limiting ---
slowapi>=0.1.9

# --- System metrics ---
psutil>=5.9.8

# --- Environment / config ---
python-dotenv>=1.0.1

# --- Utilities ---
uuid6>=2024.1.12                 # UUIDv6/v7 support (optional, stdlib uuid used in code)
